# `/src/lib/api/v1/controllers`

Contains the controller functions for API endpoints.

Each controller should contain the business logic for handling a specific resource (e.g., accounts, users, payments).
